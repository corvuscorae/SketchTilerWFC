/*
    This file exists so we don't need to put this link at the top of every file that uses Phaser
*/

import * as Phaser from "https://cdn.jsdelivr.net/npm/phaser@3.60.0/dist/phaser.esm.min.js";
export default Phaser;