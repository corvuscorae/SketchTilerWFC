// this file makes a default export for phaser from the link
// (so we dont have to have this link at the top of every file that uses phaser)
import * as Phaser from "https://cdn.jsdelivr.net/npm/phaser@3.60.0/dist/phaser.esm.min.js";

export default Phaser;